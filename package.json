{
  "name": "wow-addon-manager",
  "version": "1.0.9",
  "description": "A Windows desktop application for managing WoW addons from GitHub and GitLab",
  "author": "LoneBrownie",
  "main": "public/electron.js",
  "homepage": "./",
  "private": true,
  "dependencies": {
    "adm-zip": "^0.5.15",
    "electron-updater": "^6.6.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1"
  },
  "overrides": {
    "nth-check": "^2.0.1",
    "postcss": "^8.4.31",
    "webpack-dev-server": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "electron": "^32.2.7",
    "electron-builder": "^25.1.8",
    "wait-on": "^8.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "electron-dev": "concurrently \"npm start\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-pack": "electron-builder",
    "preelectron-pack": "npm run build",
    "dist": "npm run build && electron-builder --publish=never",
    "dist-win": "npm run build && electron-builder --win",
    "dist-portable": "npm run build && electron-builder --win --dir",
    "publish": "npm run build && electron-builder --publish=always"
  },
  "build": {
    "appId": "com.example.brownies-addon-manager",
    "productName": "Brownie's Addon Manager",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/**/*",
      "public/electron.js",
      "public/preload.js",
      "public/Logo.png",
      "node_modules/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "public/Logo.png",
      "signAndEditExecutable": false,
      "signDlls": false,
      "publish": ["github"]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false,
      "deleteAppDataOnUninstall": false,
      "include": "installer.nsh",
      "allowElevation": false,
      "createDesktopShortcut": "always",
      "createStartMenuShortcut": true,
      "installerSidebar": "public/Logo.png",
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "publish": {
      "provider": "github",
      "owner": "LoneBrownie",
      "repo": "AddonManager"
    }
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
